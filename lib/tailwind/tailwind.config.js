/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./../../**/*.php", // PHP 파일 경로 (프로젝트 루트 기준)
    // 필요한 경우 다른 HTML 파일 경로 추가
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}